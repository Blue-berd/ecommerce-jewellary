"use strict"

const handleError = async function(error){
    if(error.code === ""){
        return ``
    }
    if(error.code === ""){
        return ``
    }
    if(error.code === ""){
        return ``
    }
    if(error.code === ""){
        return ``
    }
    if(error.code === ""){
        return ``
    }
    if(error.code === ""){
        return ``
    }
    return "Internal server error"
}

module.exports = {handleError}
  